<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:id="@+id/nav_home"
        android:title="Inicio"
        android:icon="@drawable/ic_home" />  <!-- Cambia por tu ícono -->
    <item
        android:id="@+id/nav_lista_entrenamientos"
        android:title="Lista de Entrenamientos"
        android:icon="@drawable/ic_list" />  <!-- Cambia por tu ícono -->
    <item
        android:id="@+id/nav_profile"
        android:title="Perfil"
        android:icon="@drawable/ic_profile" />  <!-- Cambia por tu ícono -->
    <item
        android:id="@+id/nav_settings"
        android:title="Configuración"
        android:icon="@drawable/ic_settings" />  <!-- Cambia por tu ícono -->
    <item
        android:id="@+id/nav_about"
        android:title="Acerca de"
        android:icon="@drawable/ic_about" />  <!-- Cambia por tu ícono -->
    <item
        android:id="@+id/nav_logout"
        android:title="Cerrar sesión"
        android:icon="@drawable/ic_logout" />  <!-- Cambia por tu ícono -->
</menu>